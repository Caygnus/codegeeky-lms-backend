// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/omkar273/codegeeky/ent/internshipbatch"
	"github.com/omkar273/codegeeky/ent/predicate"
)

// InternshipBatchQuery is the builder for querying InternshipBatch entities.
type InternshipBatchQuery struct {
	config
	ctx        *QueryContext
	order      []internshipbatch.OrderOption
	inters     []Interceptor
	predicates []predicate.InternshipBatch
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InternshipBatchQuery builder.
func (ibq *InternshipBatchQuery) Where(ps ...predicate.InternshipBatch) *InternshipBatchQuery {
	ibq.predicates = append(ibq.predicates, ps...)
	return ibq
}

// Limit the number of records to be returned by this query.
func (ibq *InternshipBatchQuery) Limit(limit int) *InternshipBatchQuery {
	ibq.ctx.Limit = &limit
	return ibq
}

// Offset to start from.
func (ibq *InternshipBatchQuery) Offset(offset int) *InternshipBatchQuery {
	ibq.ctx.Offset = &offset
	return ibq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ibq *InternshipBatchQuery) Unique(unique bool) *InternshipBatchQuery {
	ibq.ctx.Unique = &unique
	return ibq
}

// Order specifies how the records should be ordered.
func (ibq *InternshipBatchQuery) Order(o ...internshipbatch.OrderOption) *InternshipBatchQuery {
	ibq.order = append(ibq.order, o...)
	return ibq
}

// First returns the first InternshipBatch entity from the query.
// Returns a *NotFoundError when no InternshipBatch was found.
func (ibq *InternshipBatchQuery) First(ctx context.Context) (*InternshipBatch, error) {
	nodes, err := ibq.Limit(1).All(setContextOp(ctx, ibq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{internshipbatch.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ibq *InternshipBatchQuery) FirstX(ctx context.Context) *InternshipBatch {
	node, err := ibq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InternshipBatch ID from the query.
// Returns a *NotFoundError when no InternshipBatch ID was found.
func (ibq *InternshipBatchQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ibq.Limit(1).IDs(setContextOp(ctx, ibq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{internshipbatch.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ibq *InternshipBatchQuery) FirstIDX(ctx context.Context) string {
	id, err := ibq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InternshipBatch entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InternshipBatch entity is found.
// Returns a *NotFoundError when no InternshipBatch entities are found.
func (ibq *InternshipBatchQuery) Only(ctx context.Context) (*InternshipBatch, error) {
	nodes, err := ibq.Limit(2).All(setContextOp(ctx, ibq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{internshipbatch.Label}
	default:
		return nil, &NotSingularError{internshipbatch.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ibq *InternshipBatchQuery) OnlyX(ctx context.Context) *InternshipBatch {
	node, err := ibq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InternshipBatch ID in the query.
// Returns a *NotSingularError when more than one InternshipBatch ID is found.
// Returns a *NotFoundError when no entities are found.
func (ibq *InternshipBatchQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ibq.Limit(2).IDs(setContextOp(ctx, ibq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{internshipbatch.Label}
	default:
		err = &NotSingularError{internshipbatch.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ibq *InternshipBatchQuery) OnlyIDX(ctx context.Context) string {
	id, err := ibq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InternshipBatches.
func (ibq *InternshipBatchQuery) All(ctx context.Context) ([]*InternshipBatch, error) {
	ctx = setContextOp(ctx, ibq.ctx, ent.OpQueryAll)
	if err := ibq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*InternshipBatch, *InternshipBatchQuery]()
	return withInterceptors[[]*InternshipBatch](ctx, ibq, qr, ibq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ibq *InternshipBatchQuery) AllX(ctx context.Context) []*InternshipBatch {
	nodes, err := ibq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InternshipBatch IDs.
func (ibq *InternshipBatchQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ibq.ctx.Unique == nil && ibq.path != nil {
		ibq.Unique(true)
	}
	ctx = setContextOp(ctx, ibq.ctx, ent.OpQueryIDs)
	if err = ibq.Select(internshipbatch.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ibq *InternshipBatchQuery) IDsX(ctx context.Context) []string {
	ids, err := ibq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ibq *InternshipBatchQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ibq.ctx, ent.OpQueryCount)
	if err := ibq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ibq, querierCount[*InternshipBatchQuery](), ibq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ibq *InternshipBatchQuery) CountX(ctx context.Context) int {
	count, err := ibq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ibq *InternshipBatchQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ibq.ctx, ent.OpQueryExist)
	switch _, err := ibq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ibq *InternshipBatchQuery) ExistX(ctx context.Context) bool {
	exist, err := ibq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InternshipBatchQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ibq *InternshipBatchQuery) Clone() *InternshipBatchQuery {
	if ibq == nil {
		return nil
	}
	return &InternshipBatchQuery{
		config:     ibq.config,
		ctx:        ibq.ctx.Clone(),
		order:      append([]internshipbatch.OrderOption{}, ibq.order...),
		inters:     append([]Interceptor{}, ibq.inters...),
		predicates: append([]predicate.InternshipBatch{}, ibq.predicates...),
		// clone intermediate query.
		sql:  ibq.sql.Clone(),
		path: ibq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InternshipBatch.Query().
//		GroupBy(internshipbatch.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ibq *InternshipBatchQuery) GroupBy(field string, fields ...string) *InternshipBatchGroupBy {
	ibq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InternshipBatchGroupBy{build: ibq}
	grbuild.flds = &ibq.ctx.Fields
	grbuild.label = internshipbatch.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//	}
//
//	client.InternshipBatch.Query().
//		Select(internshipbatch.FieldStatus).
//		Scan(ctx, &v)
func (ibq *InternshipBatchQuery) Select(fields ...string) *InternshipBatchSelect {
	ibq.ctx.Fields = append(ibq.ctx.Fields, fields...)
	sbuild := &InternshipBatchSelect{InternshipBatchQuery: ibq}
	sbuild.label = internshipbatch.Label
	sbuild.flds, sbuild.scan = &ibq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InternshipBatchSelect configured with the given aggregations.
func (ibq *InternshipBatchQuery) Aggregate(fns ...AggregateFunc) *InternshipBatchSelect {
	return ibq.Select().Aggregate(fns...)
}

func (ibq *InternshipBatchQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ibq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ibq); err != nil {
				return err
			}
		}
	}
	for _, f := range ibq.ctx.Fields {
		if !internshipbatch.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ibq.path != nil {
		prev, err := ibq.path(ctx)
		if err != nil {
			return err
		}
		ibq.sql = prev
	}
	return nil
}

func (ibq *InternshipBatchQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InternshipBatch, error) {
	var (
		nodes = []*InternshipBatch{}
		_spec = ibq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*InternshipBatch).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &InternshipBatch{config: ibq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ibq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ibq *InternshipBatchQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ibq.querySpec()
	_spec.Node.Columns = ibq.ctx.Fields
	if len(ibq.ctx.Fields) > 0 {
		_spec.Unique = ibq.ctx.Unique != nil && *ibq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ibq.driver, _spec)
}

func (ibq *InternshipBatchQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(internshipbatch.Table, internshipbatch.Columns, sqlgraph.NewFieldSpec(internshipbatch.FieldID, field.TypeString))
	_spec.From = ibq.sql
	if unique := ibq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ibq.path != nil {
		_spec.Unique = true
	}
	if fields := ibq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, internshipbatch.FieldID)
		for i := range fields {
			if fields[i] != internshipbatch.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ibq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ibq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ibq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ibq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ibq *InternshipBatchQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ibq.driver.Dialect())
	t1 := builder.Table(internshipbatch.Table)
	columns := ibq.ctx.Fields
	if len(columns) == 0 {
		columns = internshipbatch.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ibq.sql != nil {
		selector = ibq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ibq.ctx.Unique != nil && *ibq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ibq.predicates {
		p(selector)
	}
	for _, p := range ibq.order {
		p(selector)
	}
	if offset := ibq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ibq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// InternshipBatchGroupBy is the group-by builder for InternshipBatch entities.
type InternshipBatchGroupBy struct {
	selector
	build *InternshipBatchQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ibgb *InternshipBatchGroupBy) Aggregate(fns ...AggregateFunc) *InternshipBatchGroupBy {
	ibgb.fns = append(ibgb.fns, fns...)
	return ibgb
}

// Scan applies the selector query and scans the result into the given value.
func (ibgb *InternshipBatchGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ibgb.build.ctx, ent.OpQueryGroupBy)
	if err := ibgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InternshipBatchQuery, *InternshipBatchGroupBy](ctx, ibgb.build, ibgb, ibgb.build.inters, v)
}

func (ibgb *InternshipBatchGroupBy) sqlScan(ctx context.Context, root *InternshipBatchQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ibgb.fns))
	for _, fn := range ibgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ibgb.flds)+len(ibgb.fns))
		for _, f := range *ibgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ibgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ibgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InternshipBatchSelect is the builder for selecting fields of InternshipBatch entities.
type InternshipBatchSelect struct {
	*InternshipBatchQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ibs *InternshipBatchSelect) Aggregate(fns ...AggregateFunc) *InternshipBatchSelect {
	ibs.fns = append(ibs.fns, fns...)
	return ibs
}

// Scan applies the selector query and scans the result into the given value.
func (ibs *InternshipBatchSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ibs.ctx, ent.OpQuerySelect)
	if err := ibs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InternshipBatchQuery, *InternshipBatchSelect](ctx, ibs.InternshipBatchQuery, ibs, ibs.inters, v)
}

func (ibs *InternshipBatchSelect) sqlScan(ctx context.Context, root *InternshipBatchQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ibs.fns))
	for _, fn := range ibs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ibs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ibs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
