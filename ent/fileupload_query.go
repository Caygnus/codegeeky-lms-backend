// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/omkar273/codegeeky/ent/fileupload"
	"github.com/omkar273/codegeeky/ent/predicate"
)

// FileUploadQuery is the builder for querying FileUpload entities.
type FileUploadQuery struct {
	config
	ctx        *QueryContext
	order      []fileupload.OrderOption
	inters     []Interceptor
	predicates []predicate.FileUpload
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FileUploadQuery builder.
func (fuq *FileUploadQuery) Where(ps ...predicate.FileUpload) *FileUploadQuery {
	fuq.predicates = append(fuq.predicates, ps...)
	return fuq
}

// Limit the number of records to be returned by this query.
func (fuq *FileUploadQuery) Limit(limit int) *FileUploadQuery {
	fuq.ctx.Limit = &limit
	return fuq
}

// Offset to start from.
func (fuq *FileUploadQuery) Offset(offset int) *FileUploadQuery {
	fuq.ctx.Offset = &offset
	return fuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fuq *FileUploadQuery) Unique(unique bool) *FileUploadQuery {
	fuq.ctx.Unique = &unique
	return fuq
}

// Order specifies how the records should be ordered.
func (fuq *FileUploadQuery) Order(o ...fileupload.OrderOption) *FileUploadQuery {
	fuq.order = append(fuq.order, o...)
	return fuq
}

// First returns the first FileUpload entity from the query.
// Returns a *NotFoundError when no FileUpload was found.
func (fuq *FileUploadQuery) First(ctx context.Context) (*FileUpload, error) {
	nodes, err := fuq.Limit(1).All(setContextOp(ctx, fuq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fileupload.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fuq *FileUploadQuery) FirstX(ctx context.Context) *FileUpload {
	node, err := fuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FileUpload ID from the query.
// Returns a *NotFoundError when no FileUpload ID was found.
func (fuq *FileUploadQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fuq.Limit(1).IDs(setContextOp(ctx, fuq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fileupload.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fuq *FileUploadQuery) FirstIDX(ctx context.Context) string {
	id, err := fuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FileUpload entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FileUpload entity is found.
// Returns a *NotFoundError when no FileUpload entities are found.
func (fuq *FileUploadQuery) Only(ctx context.Context) (*FileUpload, error) {
	nodes, err := fuq.Limit(2).All(setContextOp(ctx, fuq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fileupload.Label}
	default:
		return nil, &NotSingularError{fileupload.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fuq *FileUploadQuery) OnlyX(ctx context.Context) *FileUpload {
	node, err := fuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FileUpload ID in the query.
// Returns a *NotSingularError when more than one FileUpload ID is found.
// Returns a *NotFoundError when no entities are found.
func (fuq *FileUploadQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = fuq.Limit(2).IDs(setContextOp(ctx, fuq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fileupload.Label}
	default:
		err = &NotSingularError{fileupload.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fuq *FileUploadQuery) OnlyIDX(ctx context.Context) string {
	id, err := fuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FileUploads.
func (fuq *FileUploadQuery) All(ctx context.Context) ([]*FileUpload, error) {
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryAll)
	if err := fuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FileUpload, *FileUploadQuery]()
	return withInterceptors[[]*FileUpload](ctx, fuq, qr, fuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fuq *FileUploadQuery) AllX(ctx context.Context) []*FileUpload {
	nodes, err := fuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FileUpload IDs.
func (fuq *FileUploadQuery) IDs(ctx context.Context) (ids []string, err error) {
	if fuq.ctx.Unique == nil && fuq.path != nil {
		fuq.Unique(true)
	}
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryIDs)
	if err = fuq.Select(fileupload.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fuq *FileUploadQuery) IDsX(ctx context.Context) []string {
	ids, err := fuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fuq *FileUploadQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryCount)
	if err := fuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fuq, querierCount[*FileUploadQuery](), fuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fuq *FileUploadQuery) CountX(ctx context.Context) int {
	count, err := fuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fuq *FileUploadQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fuq.ctx, ent.OpQueryExist)
	switch _, err := fuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fuq *FileUploadQuery) ExistX(ctx context.Context) bool {
	exist, err := fuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FileUploadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fuq *FileUploadQuery) Clone() *FileUploadQuery {
	if fuq == nil {
		return nil
	}
	return &FileUploadQuery{
		config:     fuq.config,
		ctx:        fuq.ctx.Clone(),
		order:      append([]fileupload.OrderOption{}, fuq.order...),
		inters:     append([]Interceptor{}, fuq.inters...),
		predicates: append([]predicate.FileUpload{}, fuq.predicates...),
		// clone intermediate query.
		sql:  fuq.sql.Clone(),
		path: fuq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FileUpload.Query().
//		GroupBy(fileupload.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fuq *FileUploadQuery) GroupBy(field string, fields ...string) *FileUploadGroupBy {
	fuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FileUploadGroupBy{build: fuq}
	grbuild.flds = &fuq.ctx.Fields
	grbuild.label = fileupload.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status string `json:"status,omitempty"`
//	}
//
//	client.FileUpload.Query().
//		Select(fileupload.FieldStatus).
//		Scan(ctx, &v)
func (fuq *FileUploadQuery) Select(fields ...string) *FileUploadSelect {
	fuq.ctx.Fields = append(fuq.ctx.Fields, fields...)
	sbuild := &FileUploadSelect{FileUploadQuery: fuq}
	sbuild.label = fileupload.Label
	sbuild.flds, sbuild.scan = &fuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FileUploadSelect configured with the given aggregations.
func (fuq *FileUploadQuery) Aggregate(fns ...AggregateFunc) *FileUploadSelect {
	return fuq.Select().Aggregate(fns...)
}

func (fuq *FileUploadQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fuq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fuq); err != nil {
				return err
			}
		}
	}
	for _, f := range fuq.ctx.Fields {
		if !fileupload.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fuq.path != nil {
		prev, err := fuq.path(ctx)
		if err != nil {
			return err
		}
		fuq.sql = prev
	}
	return nil
}

func (fuq *FileUploadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FileUpload, error) {
	var (
		nodes = []*FileUpload{}
		_spec = fuq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FileUpload).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FileUpload{config: fuq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fuq *FileUploadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fuq.querySpec()
	_spec.Node.Columns = fuq.ctx.Fields
	if len(fuq.ctx.Fields) > 0 {
		_spec.Unique = fuq.ctx.Unique != nil && *fuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fuq.driver, _spec)
}

func (fuq *FileUploadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fileupload.Table, fileupload.Columns, sqlgraph.NewFieldSpec(fileupload.FieldID, field.TypeString))
	_spec.From = fuq.sql
	if unique := fuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fuq.path != nil {
		_spec.Unique = true
	}
	if fields := fuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fileupload.FieldID)
		for i := range fields {
			if fields[i] != fileupload.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fuq *FileUploadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fuq.driver.Dialect())
	t1 := builder.Table(fileupload.Table)
	columns := fuq.ctx.Fields
	if len(columns) == 0 {
		columns = fileupload.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fuq.sql != nil {
		selector = fuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fuq.ctx.Unique != nil && *fuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fuq.predicates {
		p(selector)
	}
	for _, p := range fuq.order {
		p(selector)
	}
	if offset := fuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FileUploadGroupBy is the group-by builder for FileUpload entities.
type FileUploadGroupBy struct {
	selector
	build *FileUploadQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fugb *FileUploadGroupBy) Aggregate(fns ...AggregateFunc) *FileUploadGroupBy {
	fugb.fns = append(fugb.fns, fns...)
	return fugb
}

// Scan applies the selector query and scans the result into the given value.
func (fugb *FileUploadGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fugb.build.ctx, ent.OpQueryGroupBy)
	if err := fugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FileUploadQuery, *FileUploadGroupBy](ctx, fugb.build, fugb, fugb.build.inters, v)
}

func (fugb *FileUploadGroupBy) sqlScan(ctx context.Context, root *FileUploadQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fugb.fns))
	for _, fn := range fugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fugb.flds)+len(fugb.fns))
		for _, f := range *fugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FileUploadSelect is the builder for selecting fields of FileUpload entities.
type FileUploadSelect struct {
	*FileUploadQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fus *FileUploadSelect) Aggregate(fns ...AggregateFunc) *FileUploadSelect {
	fus.fns = append(fus.fns, fns...)
	return fus
}

// Scan applies the selector query and scans the result into the given value.
func (fus *FileUploadSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fus.ctx, ent.OpQuerySelect)
	if err := fus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FileUploadQuery, *FileUploadSelect](ctx, fus.FileUploadQuery, fus, fus.inters, v)
}

func (fus *FileUploadSelect) sqlScan(ctx context.Context, root *FileUploadQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fus.fns))
	for _, fn := range fus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
